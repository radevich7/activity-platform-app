import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.activityplatform.app',
  appName: 'activity-platform-client',
  webDir: 'dist/activity-platform-client/browser'
};

export default config;
